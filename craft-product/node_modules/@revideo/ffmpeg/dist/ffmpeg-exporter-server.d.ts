import type { AssetInfo, RendererResult, RendererSettings } from '@revideo/core';
export interface FFmpegExporterSettings extends RendererSettings {
    audio?: string;
    audioOffset?: number;
    fastStart: boolean;
    includeAudio: boolean;
    output: string;
}
/**
 * The server-side implementation of the FFmpeg video exporter.
 */
export declare class FFmpegExporterServer {
    private readonly stream;
    private readonly command;
    private readonly promise;
    private readonly settings;
    private readonly jobFolder;
    private readonly audioFilenames;
    constructor(settings: FFmpegExporterSettings);
    start(): Promise<void>;
    handleFrame({ data }: {
        data: string;
    }): Promise<void>;
    generateAudio({ assets, startFrame, endFrame, }: {
        assets: AssetInfo[][];
        startFrame: number;
        endFrame: number;
    }): Promise<void>;
    mergeMedia(): Promise<void>;
    private resolvePath;
    end(result: RendererResult): Promise<void>;
    kill(): Promise<void>;
    private prepareAudio;
    private mergeAudioTracks;
    private calculateAtempoFilters;
}
//# sourceMappingURL=ffmpeg-exporter-server.d.ts.map