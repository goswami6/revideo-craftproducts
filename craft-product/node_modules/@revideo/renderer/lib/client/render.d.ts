import { Project } from '@revideo/core';
declare global {
    interface Window {
        onRenderComplete: () => void;
        onRenderFailed: (msg: string) => void;
    }
}
/**
 * Render the project.
 * @param project - The project to render.
 * @param range - The range of frames to render.
 */
export declare const render: (project: Project, workerId: number, totalNumOfWorkers: number, startInSeconds: number, endInSeconds: number, hiddenFolderId: string, videoWidth: number, videoHeight: number) => Promise<void>;
//# sourceMappingURL=render.d.ts.map