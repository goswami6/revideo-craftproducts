// node_modules/@revideo/2d/lib/jsx-runtime.js
function isClassComponent(fn) {
  var _a;
  return !!((_a = fn.prototype) == null ? void 0 : _a.isClass);
}
var Fragment = Symbol.for("@revideo/2d/fragment");
function jsx(type, config, key) {
  const { ref, children, ...rest } = config;
  const flatChildren = Array.isArray(children) ? children.flat() : children;
  if (type === Fragment) {
    return flatChildren;
  }
  if (isClassComponent(type)) {
    const node = new type({ ...rest, children: flatChildren, key });
    ref == null ? void 0 : ref(node);
    return node;
  } else {
    return type({ ...rest, ref, children: flatChildren, key });
  }
}

export {
  Fragment,
  jsx
};
//# sourceMappingURL=chunk-A5ZTJLWB.js.map
